const {KeywordRepo} = require('./keywords/keywords.repository')
const {SitesRepo} = require('./sites/sites.repository')
const {SettingsRepo} = require('./settings/settings.repository')

module.exports = {
    KeywordRepo,
    SitesRepo,
    SettingsRepo
}