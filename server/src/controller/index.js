const {KeywordsController} = require('./keywords/keywords.controller')
const {SitesController} = require('./sites/sites.controller')
const {SettingsController} = require('./settings/settings.controller')

module.exports = {
    KeywordsController,
    SitesController,
    SettingsController
}